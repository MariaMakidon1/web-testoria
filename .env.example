# ── Testoria Frontend — Environment Template ──────────────────────────
# Copy to .env and fill in the values.
# See .env.local (dev defaults) and .env.prod (production template).

# API Base URL
VITE_API_URL=/api/v1

# Mock Mode — set to true when backend is not running
VITE_MOCK_ENABLED=false
